# Copy to .env for local development. NEVER commit .env.
# URL of your deployed Cloudflare Worker proxy (see worker/ directory).
# This is NOT a secret — it's just an HTTPS endpoint. The Anthropic API key
# lives inside the worker as a Cloudflare secret, not in the client.
VITE_CLAUDE_PROXY_URL=
